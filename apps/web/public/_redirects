# Cloudflare Pages routing rules.
#
# /api/* is handled by the Pages Function at functions/api/[[path]].ts
# which proxies to the Fly.io backend. Pages Functions take precedence
# over _redirects so no rule is needed here for them.
#
# SPA fallback: every non-asset, non-API route returns index.html so
# the Svelte client-side router can render it.
/*  /index.html  200
