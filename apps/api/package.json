{
  "name": "@aiplottwist/api",
  "version": "0.1.0",
  "private": true,
  "description": "FastAPI backend for AI Plot Twist. Python sources live under app/. This package.json exists only so pnpm workspaces can route 'pnpm --filter ./apps/api <script>' calls into uv-based commands; it has no Node runtime deps.",
  "scripts": {
    "dev": "uv run uvicorn app.main:app --reload --port 8000",
    "test": "uv run pytest -q",
    "check": "uv run ruff check . && uv run mypy --strict app/",
    "format": "uv run ruff format .",
    "migrate": "uv run alembic upgrade head",
    "openapi:export": "uv run python ../../scripts/export_openapi.py",
    "issue-invite": "uv run python -m app.scripts.issue_invite",
    "revoke-invite": "uv run python -m app.scripts.revoke_invite",
    "list-invites": "uv run python -m app.scripts.list_invites",
    "bootstrap-cycle": "uv run python -m app.scripts.bootstrap_cycle",
    "replay-tick": "uv run python -m app.scripts.replay_tick",
    "kill-switch": "uv run python -m app.scripts.kill_switch"
  }
}
