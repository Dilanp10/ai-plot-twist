"""Integration tests for the director's filter (module 006 / T-009).

Tests live in a subdirectory so the per-test session fixture defined in
``conftest.py`` is scoped to this folder and does not collide with the
``session`` fixture pattern used by sibling repo tests.
"""
