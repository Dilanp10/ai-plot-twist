# AI Plot Twist API — middleware package
#
# Each middleware lives in its own module; this file is an empty package marker
# so callers can `from app.middleware.<name> import <Middleware>` without
# triggering surprise side-effects.
