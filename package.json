{
  "name": "ai-plot-twist",
  "version": "0.1.0",
  "private": true,
  "description": "Closed-beta social-narrative daily-cycle AI game. MVP under construction.",
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "install": "echo 'Use \"pnpm install\" (the built-in). This entry exists so the command is discoverable via \"pnpm run\". Python deps install via the postinstall hook.'",
    "install:api": "cd apps/api && uv sync --frozen",
    "postinstall": "cd apps/api && uv sync --frozen",
    "dev": "echo 'TODO (T-018): pnpm db:up && pnpm migrate && concurrently --kill-others-on-fail \"pnpm --filter ./apps/api dev\" \"pnpm --filter ./apps/web dev\"'",
    "test": "echo 'TODO (T-018): pnpm test:api && pnpm test:web'",
    "test:api": "pnpm --filter ./apps/api test",
    "test:web": "pnpm --filter ./apps/web test",
    "check": "echo 'TODO (T-018): pnpm check:api && pnpm check:web'",
    "check:api": "pnpm --filter ./apps/api check",
    "check:web": "pnpm --filter ./apps/web check",
    "format": "echo 'TODO (T-018): pnpm format:api && pnpm format:web'",
    "format:api": "pnpm --filter ./apps/api format",
    "format:web": "pnpm --filter ./apps/web format",
    "db:up": "echo 'TODO (T-019): docker compose -f infra/docker-compose.dev.yml up -d postgres'",
    "db:down": "echo 'TODO (T-019): docker compose -f infra/docker-compose.dev.yml down'",
    "db:reset": "echo 'TODO (T-019): pnpm db:down && docker volume rm aiplottwist_pgdata || true && pnpm db:up && pnpm migrate'",
    "migrate": "echo 'TODO (T-019): pnpm --filter ./apps/api migrate'"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "prettier": "^3.0.0"
  }
}
